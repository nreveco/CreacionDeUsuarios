CREATE TABLE USERS(ID UUID PRIMARY KEY, NAME VARCHAR(255), EMAIL VARCHAR(255), PASDWORD VARCHAR(255), CREATED DATE, MODIFIED DATE, LAST_LOGIN DATE, TOKEN VARCHAR(500), ISACTIVE BOOLEAN);
CREATE TABLE PHONES(ID INT PRIMARY KEY auto_increment, NUMBER VARCHAR(255), CITY_CODE VARCHAR(1), COUNTRY_CODE VARCHAR(2), USER_ID UUID);